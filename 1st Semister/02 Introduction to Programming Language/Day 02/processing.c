#include<stdio.h>

int main() {
    int a, b;
    printf("Enter Tow Number: ");
    scanf("%d%d", &a,&b);
    int sum;
    sum = a + b;
    printf("Them sum of tow number is %d", sum);

    return 0;
}
